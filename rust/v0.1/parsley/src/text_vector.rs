// "<p>{}</p>"
// 	in rust (for now) position is just two indexes
//  whatever the type is for indexes done
/*
pub struct Position {
	x: u32;
	y: u32;
}
*/

pub struct Vector {
	origin: u32,
	target: u32,	
}


